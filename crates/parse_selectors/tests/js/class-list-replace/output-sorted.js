a.classList.replace('b', 'a');
b.classList.replace("a", "a");
c.classList.replace(`c`, `d`);
d
	.classList
	.replace(
		'e',
		'f',
	);

e.classList.replace(foo, 'a');
f.classList.replace('b', foo);
g.classList.replace(foo, bar);

a.classList.replace('b', 'a');
a.classList.replace('b', 'a');
a.classList.replace('b', 'a');
a.classList.replace('b', 'a');
