a.classList.replace('a', 'b');
b.classList.replace("b", "b");
c.classList.replace(`c`, `d`);
d
	.classList
	.replace(
		'e',
		'f',
	);

e.classList.replace(foo, 'b');
f.classList.replace('a', foo);
g.classList.replace(foo, bar);
